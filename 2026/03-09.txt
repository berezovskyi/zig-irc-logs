1773049323
mountaingoat!~mountaing@user/mountaingoat
how come this concatenation for a format tuple fails? https://zig.godbolt.org/z/68WqoPGq6 it seems like they need to all be u8s, but i think i've also seen ++ used to concatenate tuples of disparate types

1773050505
chmod222!~chmod222@user/chmod222
The issue is that one isn't a format tuple, but an array

1773050521
chmod222!~chmod222@user/chmod222
If you return `struct { u8, u8, u8, u8 }` it'll work

1773050742
chmod222!~chmod222@user/chmod222
`array ++ anything` requires `anything` to be the same array; `tuple ++ anything` requires `anything` to be another tuple

1773074710
mountaingoat!~mountaing@user/mountaingoat
chmod222: i see, thanks

